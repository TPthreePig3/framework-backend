# 基于spring boot的web后端

```
CREATE USER 'threePig'@'%' IDENTIFIED BY "threePig";
CREATE DATABASE threePig DEFAULT CHARACTER SET utf8;
GRANT ALL PRIVILEGES ON threePig.* TO threePig@'%';
FLUSH PRIVILEGES;
```